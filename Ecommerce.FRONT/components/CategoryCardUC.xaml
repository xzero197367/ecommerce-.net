<UserControl x:Class="Ecommerce.FRONT.components.CategoryCardUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" >
    <Border 
        BorderThickness="1" Width="150" BorderBrush="LightGray" 
        CornerRadius="12" Margin="10" Background="White" Cursor="Hand"
        >
            <StackPanel>
                <Image Source="{Binding ImagePath}" Height="120" Stretch="Uniform" Margin="0,0,0,10"/>
                <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="16" TextAlignment="Center" Margin="5"/>
                <TextBlock Text="{Binding Description}" Foreground="Gray" FontSize="12" TextWrapping="Wrap" TextAlignment="Center" Margin="5"/>
            </StackPanel>
    
        <!-- Bind MouseLeftButtonDown to ClickCommand -->
        <Border.InputBindings>
            <MouseBinding MouseAction="LeftClick" 
                          Command="{Binding ClickCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                          CommandParameter="{Binding Category, RelativeSource={RelativeSource AncestorType=UserControl}}" />
        </Border.InputBindings>
    </Border>
</UserControl>
