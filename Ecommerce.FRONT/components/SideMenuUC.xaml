<UserControl x:Class="Ecommerce.FRONT.components.SideMenuUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Ecommerce.FRONT.components"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
   <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" x:Name="SidebarColumn" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Sidebar -->
        <DockPanel x:Name="Sidebar" Background="#2C3E50" Width="200" >
            <DockPanel.Resources>
                <Style TargetType="Button" x:Key="MenuButtonStyle">
                    <Setter Property="Background" Value="Transparent" />
                    <Setter Property="Foreground" Value="White" />
                    <Setter Property="FontSize" Value="14" />
                    <Setter Property="Padding" Value="10" />
                    <Setter Property="Margin" Value="5" />
                    <Setter Property="BorderThickness" Value="0" />
                    <Setter Property="HorizontalContentAlignment" Value="Left" />
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#34495E" />
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DockPanel.Resources>

          

            <!-- Menu Items -->
            <StackPanel>
                <!-- Toggle Button -->
                <ToggleButton x:Name="ToggleSidebarButton" 
                              Content="☰" 
                              DockPanel.Dock="Top" 
                              FontSize="20" 
                              Padding="10" 
                              Background="Transparent" 
                              Foreground="White" 
                              BorderThickness="0"
                              Checked="ToggleSidebarButton_Checked"
                              Unchecked="ToggleSidebarButton_Unchecked"
                              
                              />
                
                <Button Content="🏠 Home" Style="{StaticResource MenuButtonStyle}" Click="MenuButton_Click" />
                <Button Content="🏠 Categories" Style="{StaticResource MenuButtonStyle}" Click="MenuButton_Click" />
                <Button Content="🏠 Products" Style="{StaticResource MenuButtonStyle}" Click="MenuButton_Click" />
                <Button Content="🏠 Cart" Style="{StaticResource MenuButtonStyle}" Click="MenuButton_Click" />
                <Button Content="🏠 Profile" Style="{StaticResource MenuButtonStyle}" Click="MenuButton_Click" />
            </StackPanel>
        </DockPanel>

        <!-- Main Content -->
        <!-- <ContentControl Grid.Column="1" Content="{Binding CurrentPage}"> -->
        <!--     <ContentControl.ContentTemplate> -->
        <!--         <DataTemplate> -->
        <!--             <pages:CategoryPageUC /> -->
        <!--         </DataTemplate> -->
        <!--     </ContentControl.ContentTemplate> -->
        <!-- </ContentControl> -->
    </Grid>
</UserControl>
