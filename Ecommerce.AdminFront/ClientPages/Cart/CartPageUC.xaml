<UserControl x:Class="Ecommerce.AdminFront.ClientPages.Cart.CartPageUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             MinHeight="120" MinWidth="300" Margin="5">
    <UserControl.Resources>
        <!-- DropShadowEffect for Card -->
        <DropShadowEffect x:Key="CardShadow" 
                         ShadowDepth="2" 
                         Direction="315" 
                         BlurRadius="8" 
                         Opacity="0.2" 
                         Color="Black"/>

        <!-- Button Style with Hover and Click Animation -->
        <Style x:Key="CartButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#007BFF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="Margin" Value="4,2"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="4" 
                                BorderThickness="0">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#0056b3"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#003d80"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Remove Button Style -->
        <Style x:Key="RemoveButtonStyle" TargetType="Button" BasedOn="{StaticResource CartButtonStyle}">
            <Setter Property="Background" Value="#DC3545"/>
            <Setter Property="Margin" Value="0,8,0,0"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#c82333"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#b21f2d"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Quantity Button Style -->
        <Style x:Key="QuantityButtonStyle" TargetType="Button" BasedOn="{StaticResource CartButtonStyle}">
            <Setter Property="Background" Value="#6C757D"/>
            <Setter Property="Width" Value="28"/>
            <Setter Property="Height" Value="28"/>
            <Setter Property="FontSize" Value="16"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#5a6268"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#4b5257"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- TextBlock Style -->
        <Style x:Key="CartTextBlockStyle" TargetType="TextBlock">
            <Setter Property="Margin" Value="0,2"/>
            <Setter Property="FontFamily" Value="Segoe UI"/>
        </Style>
    </UserControl.Resources>

    <Border Background="White" 
            BorderBrush="#E0E0E0" 
            BorderThickness="1" 
            CornerRadius="8" 
            Padding="12" 
            Effect="{StaticResource CardShadow}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="80"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Product Image -->
            <Border Grid.Column="0" 
                    CornerRadius="6" 
                    BorderBrush="#E0E0E0" 
                    BorderThickness="1" 
                    Margin="0,0,8,0">
                <Image Source="{Binding ProductImageUrl}" 
                       Stretch="Uniform" 
                       Width="70" 
                       Height="70"/>
            </Border>

            <!-- Product Details -->
            <StackPanel Grid.Column="1">
                <TextBlock Text="{Binding ProductName}" 
                           FontSize="16" 
                           FontWeight="SemiBold" 
                           Style="{StaticResource CartTextBlockStyle}"/>
                <TextBlock Text="{Binding Price, StringFormat='Price: {0:C}'}" 
                           FontSize="14" 
                           Foreground="#28A745" 
                           Style="{StaticResource CartTextBlockStyle}"/>
                <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                    <Button Content="-" 
                            Style="{StaticResource QuantityButtonStyle}" 
                            Click="DecreaseQuantity_Click"/>
                    <TextBlock Text="{Binding Quantity}" 
                               Width="40" 
                               TextAlignment="Center" 
                               FontSize="14" 
                               VerticalAlignment="Center" 
                               Style="{StaticResource CartTextBlockStyle}"/>
                    <Button Content="+" 
                            Style="{StaticResource QuantityButtonStyle}" 
                            Click="IncreaseQuantity_Click"/>
                </StackPanel>
                <Button Content="Remove" 
                        Style="{StaticResource RemoveButtonStyle}" 
                        Click="Remove_Click" 
                        Width="80"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>