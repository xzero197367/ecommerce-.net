<UserControl x:Class="Ecommerce.AdminFront.Pages.Orders.sections.OrderTableUC"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Ecommerce.AdminFront.Pages.Orders.sections"
             
             mc:Ignorable="d" >
    <Grid>
        <ListView Background="Transparent" ItemsSource="{Binding Orders}">
            <ListView.View>
                <GridView>
                    <!--<GridViewColumn Header="Order ID" Width="80"
                            DisplayMemberBinding="{Binding OrderID}" />

                    <GridViewColumn Header="User ID" Width="80"
                            DisplayMemberBinding="{Binding UserID}" />-->

                    <GridViewColumn Header="Order Date" Width="120">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding OrderDate, StringFormat=d}" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <GridViewColumn Header="Total Amount" Width="100"
                            DisplayMemberBinding="{Binding TotalAmount}" />

                    <GridViewColumn Header="Status" Width="100"
                            DisplayMemberBinding="{Binding Status}" />

                    <GridViewColumn Header="Processed Date" Width="120">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding DateProcessed, StringFormat=d}" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>

                    <GridViewColumn Header="User Name" Width="150">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding User.FullName}" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>

    </Grid>
</UserControl>
